apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Environment-agnostic pieces. Namespace, host, image tag, and per-instance config
# live in the overlay, so a new instance is just a copy of an overlay folder.
resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - rbac.yaml
